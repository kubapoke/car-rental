# .env.example for C# Web API
# 
# INSTRUCTIONS:
# This file is a template for environment variables used in this project. 
# To use it, make a copy of this file and name it `.env`.
# The `.env` file should NOT be committed to version control, as it contains sensitive data.
# Make sure `.env` is included in `.gitignore`.
#
# Anytime you add a new environment variable, update this file (.env.example) 
# so others are informed about the required variables.
#
# NOTE: This project uses the DotNetEnv package to load variables from the `.env` file.

# Example usage:
# API key for accessing external services
# MY_SECRET_API_KEY=your-api-key-here

# Database connection string
# DATABASE_URL=your-database-url-here

# The list of currently required environment variables, with example values:
ALLOWED_ORIGINS=https://example.com,http://localhost:5173
DATABASE_CONNECTION_STRING=
CAR_RENTAL_SECRET_KEY=
AZURE_BLOB_CONNECTION_STRING=
BLOB_CONTAINER_NAME=