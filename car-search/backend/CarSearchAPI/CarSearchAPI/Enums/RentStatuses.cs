namespace CarSearchAPI.Enums
{
    public enum RentStatuses
    {
        // This is different than the RentStatus in CarRental which has an additional "ReadyToReturn" state
        Active = 1,
        Returned = 2
    }
}
